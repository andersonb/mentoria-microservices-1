server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5432/pg-order-service
    username: order-service-app
    password: order-service-app
  jpa:
    hibernate:
      ddl-auto: update

#      spring.jpa.database=POSTGRESQL
#      spring.datasource.platform=postgres
#      spring.jpa.show-sql=true
#      spring.jpa.hibernate.ddl-auto=create-drop
#      spring.database.driverClassName=org.postgresql.Driver
#      spring.datasource.url=jdbc:postgresql://localhost:5432/priview
#      spring.datasource.username=postgres
#      spring.datasource.password=postgres
#      server.port=8080



#  logging.level.org.springframework=INFO
#  logging.level.com.mkyong=INFO
#  logging.level.com.zaxxer=DEBUG
#  logging.level.root=ERROR
#
#  spring.datasource.hikari.connectionTimeout=20000
#  spring.datasource.hikari.maximumPoolSize=5
#  spring.datasource.hikari.poolName=HikariPoolZZZ
#
#  logging.pattern.console=%-5level %logger{36} - %msg%n